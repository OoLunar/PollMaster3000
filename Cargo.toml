[package]
name = "poll_master3000"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "=0.5.0-rc.3", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
ulid = { version = "1.0.0", features = ["serde", "uuid"] }